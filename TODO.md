# CEOBE TODO

## 侧栏整理聊天（待视觉验收）

- [x] 官方“最近”标题栏、收起/展开、新会话导入入口与整理聊天菜单。
- [x] 按列表/按项目切换、勾选、项目分组及官方会话缩进/空状态。
- [x] 项目编辑图标进入详情，项目更多按用户要求暂缓。
- [x] 浏览器偏好保存、键盘/外部关闭、开发生产与既有聊天/详情/归属标签回归。

## 项目详情（框架已获用户认可）

- [x] 复原侧栏聊天标题后的官方项目名称小字，真实关联同步、无归属不显示、长名称截断与静态首屏覆盖。

- [x] 项目列表点击/键盘进入项目详情，按真实关联显示会话标题、摘要和日期。
- [x] 冻结“项目详细”官方 DOM/CSS；顶部 Composer 替换为导入会话并自动关联当前项目。
- [x] 复用聊天行更多操作，重命名/置顶/移至项目后同步视图。
- [x] 验证关联失败后只重试关联、无重复导入；无效项目拒绝导入；开发/生产及官方关键样式对照。
- [ ] 项目分享、来源及设置（本轮未扩展，需要对应官方 DOM 和功能授权）。

## 官方 HTML 视觉还原

- [x] 补齐 ChatGPT 页面引用的官方 SVG sprite 资源，并将 `/cdn/assets/...` 改写为本地路径。
- [x] 以保存的官方 HTML、DOM 层级和 CSS 为视觉实现主体，不再手写近似版 ChatGPT 界面。
- [x] 合并顶部、中部、底部三份 HTML 中捕获到的官方 message DOM 模板。
- [x] 让 HTML renderer 直接读取 CEOBE JSON 的 typed messages；仅在普通正文块内部使用 Markdown 排版。
- [x] 保留官方侧栏、顶部区域、Composer、消息操作区等组件的静态外观；第一阶段不实现按钮功能。
- [x] 为 Markdown 补齐 LaTeX、软换行、代码高亮、表格和结构化图表的静态渲染。
- [x] 以 ChatGPT Share HTML 中的引用元数据恢复网页引用与文件引用的精确挂载位置；Markdown 仅作为有损后备来源。
- [x] 建立 Share HTML → CEOBE JSON importer，保留完整消息节点、元数据、引用、附件指针和结构化 `genui` 图表。
- [x] 将重建消息中的文件/网页引用标签替换为官方 HTML 克隆的 DOM，移除近似标签 CSS；保留元数据供详情使用。
- [x] 接入官方右侧文件预览面板：附件卡片/文件引用打开，关闭按钮/Escape 关闭；网页引用仍直接链接来源网页。
- [x] TXT、DOCX 使用保存的官方正文 DOM；PDF 从原文件生成全部 7 页；XLSX 读取两张工作表并支持切换。
- [x] 粘贴文本使用保存的官方弹窗正文，从附件卡片及引用打开；支持内部滚动、关闭和 Escape，不套用右侧文件面板。
- [x] 根据“粘贴的文本2”区分入口：附件卡片打开弹窗，正文引用打开官方右侧代码预览。
- [x] 以用户提供的 374 行剪贴板恢复文本补齐粘贴文本预览；原文原样保存，预览层恢复可确认的 Gradle 引用块，并明确记录它不是 31,633 字节原附件的逐字节副本。
- [x] 用户视觉验收文件预览；当前 XLSX 以本地只读网格呈现，满足本阶段归档阅读目标。
- [x] 将四个测试文件的显式映射推广为通用附件解析与预览；按资源 ID、library ID、指针及 MIME/扩展名自动关联，未提供原文件的引用保留元数据但不伪造预览。

## 长对话归档

- [x] 阅读器接收 `--input`，移除固定消息数量/turn 编号，默认按 JSON 重建正文；样本资源仅通过显式演示入口使用。
- [x] 将 Markdown 与 HTML 拆为两个平级输出端；HTML 路径不再消费 Markdown 导出投影。
- [x] 从 typed HTML view 逐项恢复分支来源、停止思考、图片生成、结构化图表、表格和上传附件的官方 DOM。
- [x] 从保存页面中抽取独立官方视觉模板包；普通 renderer 不再依赖“测试消息/图表/新界面”源目录。
- [x] 将六种样本文件预览 DOM 与专用 CSS 迁入模板包，去除 `--sample-assets` 对“文件”源目录的依赖。
- [x] 将已下载的 PDF、DOCX、XLSX 和 TXT 归档资源按 JSON 元数据自动接入官方预览 DOM。

- [x] 处理 ChatGPT 长对话的虚拟化渲染：分别审计结构化载荷与可见 DOM，HTML renderer 永远从规范化 JSON 重建全部可见 turns。
- [ ] 研究保存前自动滚动整个 conversation、分段捕获 DOM 或从其他数据源补齐全部 turns 的方案。
- [x] 无论 HTML 中存在多少已渲染消息，都不得将其误判为完整 conversation；JSON 独立记录 structured payload 与 rendered DOM 的完整性状态。

## 测试样本

- [x] 建立包含文本、Markdown、代码、表格、图片、附件、引用、公式、超长 User 消息及工具结果的官方 ChatGPT 测试 conversation。
- [x] 建立精简且可复现的验收基线：保留原始分享响应、CEOBE JSON、Markdown 副本、关键 DOM fixture、预览输入和冻结模板包；不再把浏览器“完整网页”资源文件夹作为仓库依赖。

## 导入页体验

- [x] 来源面板缺失时跳过初始化，使用纯色 MC 字母头像替代个人头像图片。
- [x] 完善空输入禁用、链接校验、纯文本粘贴、防重复提交、等待提示、失败重试与完成摘要。
- [x] 调整导入说明，避免承诺分享页面未提供的资源也能完整保存。
- [ ] 评估侧栏静态入口的本地语义：保留官方布局时提供不可用反馈，或按本地功能范围精简；本轮不调整。

## 后续增强

### 项目页官方 DOM 复原
- [x] 构建阶段移除侧栏“已安排”和“插件”，避免首屏闪回。
- [x] 将官方项目页 DOM 和样式冻结为独立模板，接入侧栏及生产构建；普通构建不读取参考文件夹。
- [x] 参考 DOM 复原阶段完成；按后续新建功能请求移除两条参考记录，列表改读真实本地项目。
- [x] 从新建项目参考页冻结弹窗、30 个图标和颜色选择器；实现名称校验、重复提交保护、自定义颜色及名称搜索。
- [x] 开发及预览服务接入本地 JSON 项目存储；纯静态服务显示只读提示，禁止伪成功创建。
- [x] 临时目录隔离测试创建、同名冲突、持久化、搜索、筛选、键盘关闭、HTML 转义和损坏文件保护。
- [ ] 用户视觉验收新建弹窗；另行讨论项目详情、会话／资料库关联、编辑和删除；暂不启用共享及 AI 记忆。

### 聊天更多菜单
- [x] 冻结官方主菜单和移至项目子菜单，接入 9 个页面，修正侧栏参考聊天 ID 与按钮标题。
- [x] 子菜单实时读取本地项目；支持聊天独占项目关联持久化、键盘导航、外部点击关闭和视口边界定位。
- [x] 开发与生产预览验证项目同步、隔离移动保存、刷新保留、图标资源和嵌套会话路径。
- [x] 按参考 DOM/定位 JSON 恢复两层官方定位容器、内联变量、项目行包装及分隔线；撤销自定义外观覆盖和置灰处理。
- [x] 独立官方基线对照：开发/生产预览的 DOM 层级、类名、菜单项计算样式、尺寸、锚点偏移均通过；取消鼠标打开时默认高亮“分享”。
- [x] 保留“测试”项目实时同步与隔离移动保存回归；修复生产合并脚本中的命名冲突。
- [x] 从新参考冻结行内重命名、删除确认弹窗和置顶分组/行 DOM；接入名称保存、置顶和取消置顶，无额外置顶确认。
- [x] “新项目”直接复用创建弹窗并自动移入当前聊天；移除归档项，分享暂缓。
- [x] 永久删除全部归档版本、项目关联及派生页面；私有预构建、工作区写入锁、失败保护和中断恢复记录。
- [x] 开发/生产交互测试与隔离真实删除测试（含最后一条聊天、目录联接拒绝、共享附件副本保留）。
- [ ] 用户视觉验收新增界面；分享与项目详情浏览后续讨论。

### 本地资料库
- [x] 抽取官方资料库列表外壳与空白文件行，普通构建不读取参考文件目录。
- [x] 汇总各会话最新可用归档的附件，以内容哈希去重并保留全部来源；校验本地文件后开放下载。
- [x] 实现文件名／来源会话搜索、图片／文件分类、名称／归档时间／大小排序、预览及来源跳转。
- [x] 开发与生产构建资料库浏览器验证：1092×935 CSS px，DPR 1.5。
- [ ] 用户视觉验收资料库列表；网格视图与高级筛选留待后续。
- [ ] 另行确定文件夹管理、上传、新建及删除的权限和数据语义；第一阶段不提供写入操作。

- [ ] 补齐导入入口的生产发布策略：当前 `import.html` 未进入 Vite 多页构建入口，`/api/archive-share` 仅由开发服务提供；静态部署需明确禁用提示或独立服务方案。
- [ ] 核查生产构建中的 `/cdn/assets/KaTeX_*.woff2` 未解析警告，并验证离线字体和按需加载附件片段的资源路径。
- [x] 开发服务导入页浏览器验收：空输入、非法链接、纯文本粘贴、防重复提交、失败重试、资源缺失摘要及真实分享链接导入成功。测试入口：`node scripts/test-link-import-browser.mjs`；显式设置 `CEOBE_TEST_SHARE_URL` 才会执行真实归档。
- [ ] 生产预览场景的导入入口及不可用提示验收。

- [x] 本轮浏览器基线：Edge、浅色主题、视口 1092×935 CSS px、deviceScaleFactor 1.5、导入页顶部。系统缩放与浏览器缩放的拆分值未单独确认，视觉以用户截图为准。
- [ ] 评估更接近官方 Canvas 的 XLSX 交互预览；只读网格继续作为稳定离线后备。
- [ ] 为右侧对话目录的左侧区域设计本地书签功能；该区域在书签方案确定前不用于摘要浮层。
