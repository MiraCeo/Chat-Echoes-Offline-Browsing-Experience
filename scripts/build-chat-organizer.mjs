import{readFile,writeFile,readdir,copyFile}from'node:fs/promises';import{join}from'node:path';import{parseHTML}from'linkedom';import{htmlSafeSvg}from'./serialize-html.mjs';
export async function buildChatOrganizer(root){const base=join(root,'replay'),f=JSON.parse(await readFile(join(root,'official-templates/chat-organizer.json'),'utf8'));
for(const name of ['chat-organizer.js','chat-organizer.css'])await copyFile(join(root,'scripts',name),join(base,name));for(const name of f.stylesheets)await copyFile(join(root,'official-templates/assets',name),join(base,'assets',name));
const pages=(await readdir(base)).filter(n=>n.endsWith('.html')).concat((await readdir(join(base,'conversations'))).filter(n=>n.endsWith('.html')).map(n=>'conversations/'+n));
for(const page of pages){const d=parseHTML(await readFile(join(base,page),'utf8')).document,prefix=page.startsWith('conversations/')?'../':'./',recent=d.querySelector('[data-chat-recent-list]');if(!recent)continue;const block=recent.closest('.group\\/sidebar-expando-section');block.dataset.organizerRecent='';const header=parseHTML(f.listHeader).document.firstElementChild;block.firstElementChild.replaceWith(header);header.dataset.organizerHeader='';
const section=parseHTML(f.projectSection).document.firstElementChild;section.dataset.organizerProjects='';section.hidden=true;block.before(section);
const bank=d.createElement('div');bank.hidden=true;bank.id='ceobe-organizer-bank';for(const key of ['closedSvg','openSvg','projectRow','nested','empty']){const box=d.createElement('div');box.dataset.organizerTemplate=key;box.innerHTML=f[key];bank.append(box)}d.body.append(bank);
const menu=parseHTML(f.menu).document.firstElementChild;menu.dataset.organizerPopup='';menu.hidden=true;menu.querySelector('[role=menu]').id='ceobe-organizer-menu';menu.querySelector('[role=menu]').dataset.state='closed';d.body.append(menu);
for(const e of d.querySelectorAll('[data-organizer-header] a,[data-organizer-projects] a')){e.href=prefix+'import.html';e.setAttribute('aria-label','导入会话')}
for(const [i,b]of [...d.querySelectorAll('[data-organizer-header] button[aria-label="整理聊天"],[data-organizer-projects] button[aria-label="整理聊天"]')].entries()){b.id='organize-trigger-'+i;b.dataset.organizerTrigger='';b.dataset.state='closed';b.setAttribute('aria-expanded','false');b.setAttribute('aria-controls','ceobe-organizer-menu')}
for(const use of d.querySelectorAll('[data-organizer-header] use,[data-organizer-projects] use,#ceobe-organizer-bank use,[data-organizer-popup] use')){const h=use.getAttribute('href');use.setAttribute('href',prefix+'cdn/assets/'+(h.includes('sprites-shell')?'sprites-shell-097001e7.svg':'sprites-core-26c3f2d4.svg')+'#'+h.split('#')[1])}
for(const name of [...f.stylesheets,'../chat-organizer.css']){const href=prefix+'assets/'+name;if([...d.querySelectorAll('link[rel=stylesheet]')].some(e=>e.getAttribute('href')===href))continue;const l=d.createElement('link');l.rel='stylesheet';l.href=href;d.head.append(l)}
await writeFile(join(base,page),'<!DOCTYPE html>\n'+htmlSafeSvg(d.documentElement.outerHTML));}}
